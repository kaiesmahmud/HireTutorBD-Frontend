import React from 'react';
import { useParams } from 'react-router-dom';

const Profile = () => {

    return (
        <div>
            Profile Section 
        </div>
    );
};

export default Profile;